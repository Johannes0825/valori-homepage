import { Suspense } from "react";
import ConfirmForm from "@/Timer/ConfirmForm";

export default function ConfirmPage() {
    return (
        <Suspense>
            <ConfirmForm />
        </Suspense>
    );
}
